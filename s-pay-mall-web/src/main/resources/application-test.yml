server:
  port: 8092
  tomcat:
    max-connections: 20
    threads:
      max: 20
      min-spare: 10
    accept-count: 10

# 日志
logging:
  level:
    root: info
  config: classpath:logback-spring.xml